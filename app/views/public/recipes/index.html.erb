<div class="container">
  <div class="row">
    <div class="col-12">
      <% @recipes.each do |recipe| %>
        <div class="card" style="width: 10rem;">
          <%= image_tag recipe.recipe_image, size:'158x158', class: "card-image-top"%>
          <div class="card-body">
            <p class="card-text"><%= recipe.title%></p>
            <p class="card-text"><%= recipe.customer.nickname %></p>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
